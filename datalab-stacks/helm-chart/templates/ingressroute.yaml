apiVersion: traefik.containo.us/v1alpha1
kind: IngressRoute
metadata:
  name: {{ include "datasciencelab.fullname" . }}-jupyter
  labels:
    {{- include "datasciencelab.labels" . | nindent 4 }}
spec:
  entryPoints:
    - web
    - websecure
  routes:
  - kind: Rule
    match: Host(`localhost`) && PathPrefix(`/{{ .Release.Name }}`)
    services:
    - kind: Service
      name: {{ include "datasciencelab.fullname" . }}-jupyter
      port: 80
